<?php
$tab = []; //  pour l'instant le tableau est vide

//Je remplis le tableau au hasard, je mets 10 entiers dedans
for($i = 0; $i <10;$i++) {
    if($i <10){
        irand(0, 1000);
        echo '</br>';
    }

}




// Recherche du plus petit
$min = $tab[0]; // Il faut penser à initialiser min, pour l'instant, on prend le premier élément...
/*
for( complétez ) {
    // Si l'élément courant est pus petit que le min alors ...
}

echo "<table><tr>";
// Affichez sous forme de table HTML et mettez le(s) plus petit(s) élément(s) en gras.
for() {
    
}
echo "</tr></table>";

$tab2 = []; // je crée le nouveau tableau, il est vide

for() { // je parcours le premier tableau
    // si l'élément courant n'est pas min, je l\‘ajoute au nouveau tableau
}

// J'affiche le nouveau tableau
echo "<table><tr>";
for() {
    
}
echo "</tr></table>";
*/
?>